module path from 'path';
module fs from 'fs';
module Buffer from 'buffer';

module images from 'images';

class Puzzle {
  constructor() {
    //
  }
  解析(css列表, 根路径, 映射) {
    css列表.forEach(function(css) {
      css.背景列表.forEach(function(背景) {

      });
    });
  }
}

export default Puzzle;